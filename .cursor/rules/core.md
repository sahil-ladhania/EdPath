# Core Rules (Cursor — always-on)

Static commands, patterns, and canonical examples for Cursor. Keep short — every line is loaded into every prompt.

> Placeholder — to be filled in once project conventions are set.
