# System Overview

The canonical system-architecture overview lives at [`../reference/architecture.md`](../reference/architecture.md) (with the agent/graph design in [`../reference/agent-architecture.md`](../reference/agent-architecture.md)).

This file is intentionally a **pointer, not a second overview** — duplicating the root doc would only cause drift. For a code-level map of where things live as the implementation grows, see [`repo-map.md`](./repo-map.md).
