#!/usr/bin/env bash
# Single stable verification target for the repo (install, lint, typecheck, test, build).
# Placeholder — real verification commands to be added once the stack is set up.
set -euo pipefail

echo "verify.sh: no checks defined yet"
